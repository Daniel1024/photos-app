import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.testcams.app',
  appName: 'photos-app',
  webDir: 'dist/photos-app',
  bundledWebRuntime: false
};

export default config;
